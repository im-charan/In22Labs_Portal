{
  "name": "in22labs_portal",
  "version": "1.0.0",
  "scripts": {
    "build": "if-env TYPE=server && (cd server && npm install) || (cd client && npm install)",
    "server": "node ./server/server.js",
    "client": "cd client && npm start"
  },
  "dependencies": {
    "dayjs": "^1.11.13",
    "if-env": "^1.0.4",
    "request-ip": "^3.3.0"
  }
}
